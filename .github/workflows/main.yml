name: Deploy to FTP
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v3
      - name: Upload via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ ftpupload.net }}
          username: ${{ if0_41271108 }}
          password: ${{ Bb1ToMvkTf }}
          local-dir: ./  # Change this if your web files are in a subfolder
          server-dir: ./htdocs/