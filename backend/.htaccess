# Deny direct access to all files in this directory
Order deny,allow
Deny from all

# Allow access to all PHP API endpoints
<FilesMatch "\.php$">
    Allow from all
</FilesMatch>

# Explicitly block sensitive configuration files
<FilesMatch "\.json$">
    Deny from all
</FilesMatch>
